# Azure Functions runtime
azure-functions

# Azure SDK (storage, queues, tables, key vault)
azure-storage-blob
azure-storage-queue
azure-data-tables
azure-identity
azure-keyvault-keys
azure-keyvault-certificates

# Graph API (delegated auth con refresh token)
msal
requests

# XML/XSD processing y firma
lxml
cryptography

# Utilidades
python-dateutil
